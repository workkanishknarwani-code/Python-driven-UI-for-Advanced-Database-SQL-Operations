In this project, I connected Python and SQL using the `mysql.connector` library to build a Python-driven interface for performing advanced database operations.
In this UI end user can do changes in the database, without writing any SQL query.
## What I Practiced Today
- Basic SQL queries
- GROUP BY and COUNT
- LIMIT usage
